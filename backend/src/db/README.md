# Database Setup

Run `schema.sql` in Supabase SQL editor to provision Phase 2 tables, indexes, and RLS policies for the Real-Time Stock Market System.

Recommended order:
1. Enable `uuid-ossp` extension.
2. Create tables and indexes.
3. Apply RLS policies.

All timestamps are UTC and numeric fields use fixed precision for monetary values.
